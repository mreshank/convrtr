/**
 * The texture layer's import surface. Holds `ShaderSurface` for now; task 3
 * appends the three named shaders (`heroGlow`, `halftone`, `branchNetwork`)
 * v2's Graphics & Effects section names, once they exist to export.
 */
export { ShaderSurface } from "./ShaderSurface";
